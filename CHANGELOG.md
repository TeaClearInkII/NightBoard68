# NightBoard68 更新日志（PR 草稿）

> 本文档基于当前工作区相对 `origin/main` 的全部改动整理，供 PR 提交与发布说明使用。
> 建议 PR 标题：`feat(keyboard): numpad/soft-keyboard/combo overhaul + custom dialogs + touchpad drag`

---

## 一、数字小键盘

- **真实电脑小键盘布局**：标准 4×5 配列（NumLk / ÷ / × / − / +（跨 2 行）/ Ent（跨 2 行）/ 0（跨 2 列）/ .），支持键格跨行 `rowSpan` 计算。
- **真小键盘 HID 键码**：使用 Keypad 页专用码（NumLock 0x53、÷ 0x54、× 0x55、− 0x56、+ 0x57、Keypad Enter 0x58、7~0 0x59~0x62、. 0x63），电脑端识别为数字小键盘而非主键盘区。
- **悬浮窗大小可调**：右下角三斜线把手拖拽缩放（18~46 dp/格，锚定左上角），位置与大小均持久化。
- **NumLock 关闭操作标注（主机权威）**：数字键右上角小字标注通用映射（7→Home、8→↑、9→PgUp、4→←、6→→、1→End、2→↓、3→PgDn、0→Ins、.→Del）。
  - 收到主机 LED 且确认 NumLock 关 → 标注**橙色高亮**（像 Fn 激活）；
  - 未知（未收到 LED）或 NumLock 开 → 灰色常驻，**不猜测显示**。

## 二、LED 状态（主机权威）

- **全量解析**：CapsLock（bit1）与 NumLock（bit0）状态来自电脑 LED 回传——蓝牙走 HID `onSetReport`（Output Report），局域网走 Agent `{"t":"led","c":掩码}`（含 1s 心跳兜底）。
- **主机权威**：`ledKnown=false`（未收到任何 LED 消息）→ 不显示任何锁定高亮，让用户自行判断；不做本地状态翻转猜测。
- Caps 键已有 LED 高亮（橙框），新增小键盘 NumLock 标注联动。

## 三、软键盘（Moonlight 式）

- **无输入框**：顶部「软键盘」按钮唤起系统输入法，隐藏 1×1 透明 EditText 捕获输入，不再显示任何输入框。
- **中文/任意字符发送**：以字符串逐字发送，不走 HID 键码；新增局域网协议 `{"t":"txt","s":…}`，PC 端 Agent 用 `SendInput + KEYEVENTF_UNICODE` 直接打出任意 Unicode（含中文、emoji）。
- **拼音合成区过滤**：未确认的拼音/候选词不发送，选字后才发出；退格自动同步、粘贴整段发送、换行映射回车。
- **蓝牙模式自动隐藏入口**：文本只能走局域网通道，蓝牙模式下状态条不显示「软键盘」按钮。

## 四、触控板

- **鼠标键列按住联动拖放**：按住鼠标键列（左/中/右）不松 + 触控板移动 = 拖放/框选；位移与滚轮报告携带按住的键位，快速点按仍是普通点击。
- **悬浮触控板大小可调**：右下角缩放把手（110~460 dp 宽），位置与大小持久化。
- **修复双指移动事件丢失**：`ACTION_MOVE` 的 actionIndex 恒为 0，鼠标键列手指（触控板外）会导致触控板手指移动被过滤——改为 MOVE/CANCEL 直接路由给引擎。

## 五、组合键

- **垂直滚动 + 宽度自适应**：预设/自定义组合按钮流式排列（FlowLayout），宽度自适应，不再全宽；删除描述性标题文本。
- **自定义组合可新增/删除**：「＋ 新增」保存多个自定义组合按钮，长按带删除确认弹窗。
- **竖屏式编辑器（主键数量不限）**：勾选修饰键 + 下拉选主键，可「＋ 添加主键」多行组合（同时按下），行内 ✕ 删除；**键值去重**（防止 A+A+A）；兼容旧版单键存储。
- **更多可选键值**：字母/数字/F1-F12/方向键 + 数字小键盘扩展。
- 空格键显示为 `Space` 而非空串。

## 六、统一自定义弹窗

- **新 PanelDialog 框架**（替代系统 AlertDialog）：深色圆角面板，所有弹窗统一风格；弹窗窗口沉浸式，不再意外弹出导航栏。
- **右上角 ✕ 关闭按钮**：36×36dp 按钮样式（此前是文字、难按中）；标题行右对齐。
- **超高可滚动**：内容超过 92% 屏高时整体滚动，底部「保存/完成」按钮始终可达。
- **底部白条选择**：长按「中英」「输入法」「触控板模式」改为屏幕底部白色圆角选项条（点选即生效、点外收起）。

## 七、主界面与设置

- **主界面按钮统一样式 + SVG 图标**：圆角深色底、白字、图标（键盘/触屏手机/刷新/闪电/齿轮/电脑/耳机/鼠标/蓝牙）；「图标+文本」整体真居中（透明占位图标对称）。
- **已配对设备真实类型图标**：按蓝牙设备大类自动选择（电脑/手机/耳机/键盘/鼠标/其他=蓝牙）。
- **当前连接设备高亮**：橙色底 + 「（当前连接）」标注。
- **设置页卡片式布局**：每类设置一块圆角深色卡片，顶部「‹ 返回」，IP 输入框圆角化。
- **首次默认显示键盘**：触控板默认关闭（`pad_enabled` 默认 false），用户手动开过则保持其选择。

## 八、连接与键位

- **蓝牙目标切换（switchHost）**：切换已配对电脑时先断开旧主机再连新目标（HID profile 同时只维持一台主机）。
- **Fn 层调整**：PgUp→Ins、End→Home、Esc Fn 层为 `` ` ``，PgDn 无 Fn 层。
- **Mac 键位**：修饰键标签 ⌃⌥⌘（设置页「键盘布局」切换）同步到自定义快捷键预览。
- **竖屏页沉浸式/屏幕常亮**：与横屏对齐（API 30+ InsetsController、设置页开关生效）。
- **竖屏顶部边距只下移**：不再底部额外留白。

## 九、构建与环境

- `gradle.properties`：中文目录路径跳过 AGP 检查 + Kotlin 进程内编译。
- `.gitignore`：忽略 `归档/`。

## 十、PC 端 Agent（需重新编译）

- 新增 `txt` 协议 + `SendInput KEYEVENTF_UNICODE` 文本注入（含中文/emoji）。
- 重新运行 `pc-agent/build.bat` 生成新 `NightBoardAgent.exe` 后生效。

## 十一、交互引导

- **长按引导标记**：「中英」「输入法」「触控板模式」支持长按——从未长按过 = 右上角橙色小圆点；**长按过一次后**改为按钮底部 2px 圆角浅白横线（半透明，不刺眼），持久记忆。
- **首次长按教学**：首次进入键盘页弹一次「长按小技巧」说明，只弹一次。

---

## 测试要点

1. 小键盘：布局/跨行键/缩放把手/真小键盘键码（电脑端 NumLock 灯变化）；NumLock 关 → 右上角标注橙色高亮，未收到 LED → 保持灰色。
2. 软键盘：局域网模式唤起输入法，中文逐字发送、退格、粘贴整段、蓝牙模式入口隐藏。
3. 触控板：按住鼠标键列 + 触控板移动拖放；悬浮触控板缩放。
4. 组合键：新增多主键去重、长按删除确认、空格显示 Space。
5. 弹窗：右上角 ✕、超高滚动、底部白条选择、无导航栏弹出。
6. 引导：长按一次「中英/输入法/触控板模式」后，小黄点变底部浅白横线；首次进入弹长按教学。
